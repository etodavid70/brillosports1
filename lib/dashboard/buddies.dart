import 'package:flutter/cupertino.dart';

class Buddies extends StatelessWidget {
  const Buddies({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("this is the buddies page"),);
  }
}